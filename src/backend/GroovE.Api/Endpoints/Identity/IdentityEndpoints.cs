namespace GroovE.Api.Endpoints.Identity;

internal static class IdentityEndpoints
{
    //internal static IEndpointRouteBuilder MapIdentityEndpoints(this IEndpointRouteBuilder app) => app
    //    .MapGroup("/identity")
    //        .MapEndpoint<Login>();
}
